var table = $('#doubts-table');
var line = table.find('[data-id=<%= @doubt.id %>]');
var likeButton = line.find('.like-button');

likeButton.attr('data-method', 'post');
likeButton.data('method', 'post');
likeButton.prop('href', '<%= like_team_retrospective_doubt_path(@doubt) %>');

likeButton.html('(' + <%= @doubt.like %> + ')').blur();
